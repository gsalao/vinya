# Copy to .env and fill in from your Supabase project (Settings → API).
# Leave blank to run the site in prototype mode (form submissions are simulated).
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
